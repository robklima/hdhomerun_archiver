# hdhomerun_archiver
Automatically moves HDHomeRun DVR recordings to local/Plex storage via verified atomic downloads, then deletes them from the device. Runs unattended via launchd with Pushover alerts for failures. macOS, Python stdlib only, no dependencies.
